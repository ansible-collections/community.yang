---
- debug: msg="START community.yang.fetch junos/basic.yaml on connection={{ ansible_connection
    }}"

- debug: msg="END community.yang.fetch junos/basic.yaml on connection={{ ansible_connection
    }}"
