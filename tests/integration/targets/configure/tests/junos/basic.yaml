---
- name: Debug task
  debug:
    msg: "START community.yang.configure junos/basic.yaml on connection={{ ansible_connection }}"

- name: Debug task
  debug:
    msg: "END community.yang.configure junos/basic.yaml on connection={{ ansible_connection }}"
