---
minor_changes:
  - community.yang.configure - Since ``configure`` module is built on top of ``netconf_config`` we add a new option argument, ``netconf_config_args``, allowing passing options directly to ``netconf_config``.
